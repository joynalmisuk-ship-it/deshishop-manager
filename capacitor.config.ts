import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deshishop.manager',
  appName: 'DeshiShop Manager',
  webDir: 'dist'
};

export default config;
